import { createQuoteStoredDocument, quoteDocumentsBucket, storedQuoteDocumentContainsSupplierCost, storedQuoteDocumentIsPublicBucket } from "@stax/quote-delivery";
test("stored quote document uses private quote-documents bucket metadata", () => { const doc = createQuoteStoredDocument({ id: "doc", quoteDeliveryId: "delivery", quoteDocumentId: "quote-doc", storagePath: "quotes/doc.html", contentType: "text/html" }); expect(quoteDocumentsBucket).toBe("quote-documents"); expect(doc.customerSafe).toBe(true); expect(storedQuoteDocumentIsPublicBucket()).toBe(false); expect(storedQuoteDocumentContainsSupplierCost()).toBe(false); });
