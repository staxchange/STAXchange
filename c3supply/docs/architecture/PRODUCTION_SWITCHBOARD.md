# Production Switchboard

The production switchboard defines deployment surfaces, environment variables, health checks, readiness checks, and smoke tests.

It does not replace command-layer governance.

Protected mutations remain in `packages/commands`.
