# Simply Accounting Export Storage

Export files are stored in a dedicated storage bucket:

```txt
simply-accounting-export-files
```

Finance approval is required before a signed download URL can be created.

The signed download is for manual review/import into Simply Accounting-compatible workflows.
