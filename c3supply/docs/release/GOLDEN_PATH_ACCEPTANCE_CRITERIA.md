# Golden Path Acceptance Criteria

The governed revenue path must prove:

catalog/cart/photo intake → quote → pricing → quote document → delivery → acceptance → payment → fulfillment → billing → accounting handoff.

All protected mutations must go through `packages/commands`.
